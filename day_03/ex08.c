#include <stdio.h>
#include <string.h>

int main(void) {
    char phrase[128], temp[128];

    printf("Entrez une phrase : ");
    scanf("%[^\n]", phrase);

    int length = strlen(phrase);
    // . ? ! , '
    for (int i = 0; i < length; i++)
    {
        
    }
}